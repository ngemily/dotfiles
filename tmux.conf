# Force a reload of the config file
bind r source-file ~/.tmux.conf \; display "Reloaded ~/.tmux.conf"

# Pane creation
unbind %
bind | split-window -h
bind - split-window -v

# Pane movement
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Pane resizing
bind -r C-h resize-pane -L 5
bind -r C-j resize-pane -D 5
bind -r C-k resize-pane -U 5
bind -r C-l resize-pane -R 5

# Pane cycling
bind a select-pane -t:.+

# Window rename
setw -g automatic-rename off

# Activity monitoring
setw -g monitor-activity on
set -g visual-activity on

# Status bar
set -g status-right-length 150
set -g status-interval 10

#set -g status-bg colour234
#set -g status-right '#[fg=green]⮃ #(sh ~/scripts/power.sh) ⮃ #[fg=green]#(uptime | cut -d ',' -f 4- | cut -d ':' -f 2-)#[fg=colour245] ⮃ %d %b %R #[fg=colour13,bg=colour234,nobold] #(~/scripts/now-playing.sh) #[fg=colour254]⮂#[fg=colour16,bg=colour254,bold] #h '
#set -g status-right '#(~/scripts/playing.scpt)'

#set -g window-status-format "#[fg=white,bg=colour234] #I #W"
#set -g window-status-current-format "#[fg=colour234,bg=colour39]⮀ #[fg=colour25,bg=colour39,noreverse,bold] #I #W #[fg=colour39,bg=colour234,nobold]⮀"

#set-option -g status-left-length 10
#set-option -g status-left "#[fg=green]#H"
#set-option -g status-right-length 450
#set-option -g status-right "%I:%M %p #[fg=yellow][Bat: #(sh ~/scripts/power.sh)]#[fg=green]#(uptime | cut -d ',' -f 4-)"

source "/usr/local/lib/python2.7/site-packages/powerline/bindings/tmux/powerline.conf"
#set -g status-left '#[fg=colour235,bg=colour252,bold] … #S #[fg=colour252,bg=colour238,nobold]⮀#[fg=colour245,bg=colour238,bold] #(whoami) #[fg=colour238,bg=colour234,nobold]⮀'
set -g status-left-length 150
run-shell "powerline-daemon -q"
